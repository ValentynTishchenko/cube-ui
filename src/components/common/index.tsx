import styled from '@emotion/styled';
import { colors } from '../../styles/variables';

export const Input = styled.input`
  background: none;
  border: none;
  color: ${colors.white};
  border-bottom: 1px solid ${colors.nepal} ;
`;
